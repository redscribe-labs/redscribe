Hi {{ user.first_name|default:user.username }},

This is a confirmation that the password on your RedScribe account
({{ user.username }}) was just changed. All other active sessions for
this account have been signed out.

If you made this change, no action is needed.

If you didn't make this change, contact your RedScribe administrator
immediately.
